# Public-release checklist

## Completed in the working tree

- [x] Searched current text files for credential assignments and private-key
  markers; none detected.
- [x] Scanned 102 reachable Git objects for common credential/private-key
  patterns; no credential match was detected. One generated session contains
  a machine-specific absolute path.
- [x] Identified generated bytecode, notebook session state, local paths, and
  model binaries.
- [x] Added ignores for caches, environments, local logs, generated sessions,
  and checkpoints.
- [x] Replaced unsupported headline wording with the complete six-seed record.
- [x] Added strict checkpoint compatibility checks.
- [x] Separated the canonical non-vision experiment from historical and vision
  work in the public navigation.

## Required before clicking “Public”

- [ ] Select and add an open-source license; `LICENSE-TODO.md` is not a license.
- [ ] Decide whether the corporate-domain author email present in Git history
  is appropriate to expose. Changing current files does not remove Git-history
  metadata.
- [ ] Review old Git commits for IP, employer, or private-company material.
- [ ] Remove generated Marimo session files and bytecode from the release
  commit (their deletions are staged only when you choose to stage them).
- [ ] Confirm that the machine-specific path in historical generated session
  state is acceptable in Git history, or sanitize history before publication.
- [ ] Do not release the historical v8 checkpoint as reproducible. Generate a
  strictly compatible checkpoint, validate it, publish it as a release asset,
  and record a checksum.
- [ ] Review every scientific claim in `README.md`, `docs/`, and the result CSV.
- [ ] Add CI on at least one declared Python version before treating the project
  as maintained public software.

## History warning

Deleting a secret, private path, email, or artifact in the current tree does
not remove it from Git history. No credential was detected by the pattern scan,
but this automated check is not a substitute for a repository-owner review. If
a genuine credential is found later, revoke it first and sanitize history
before making the repository public.
